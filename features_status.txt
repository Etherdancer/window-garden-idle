================================================================================
WINDOW GARDEN IDLE - ROADMAP STEPS 1-4 STATUS REPORT
================================================================================

All features outlined in Steps 1 through 4 of the implementation plan have been
successfully and fully implemented in the codebase. Below is the verification status
for each component:

--------------------------------------------------------------------------------
[✓] Step 1: Audio & ASMR Polish
--------------------------------------------------------------------------------
- Custom wave-based procedural sound assets generated:
  - water_pour.wav (cozy bubble pour sound)
  - wipe.wav (soft friction rustle sound)
  - press.wav (gentle wooden block press sound)
- AudioService updated to support SFX player, pre-set comfortable volumes
  (bg_music: 0.3, sfx: 0.7), and playSFX one-shots.
- Interactions fully wired up in main_game_screen.dart:
  - Water Plant button triggers water SFX.
  - Wipe Dust gesture triggers wipe SFX with a 250ms rate-limiting throttle.
  - Harvest & Press action triggers press SFX.

--------------------------------------------------------------------------------
[✓] Step 2: Dynamic Weather Simulator
--------------------------------------------------------------------------------
- Created WeatherState model (sunny, cloudy, rainy).
- Implemented WeatherService for deterministic, time-based weather cycles
  changing every 3 hours (100% offline-compatible).
- Added currentWeather getter to the Garden model.
- TimeManager updated to scale sunlight and moisture depletion rate:
  - Cloudy: 40% light intensity.
  - Rainy: 10% light intensity, 50% evaporation rate (high humidity).
- Added WeatherBadge UI to main_game_screen.dart windowsill location header.
- Scaled real-time sunlight meter with weather coefficients.

--------------------------------------------------------------------------------
[✓] Step 3: Push Notifications
--------------------------------------------------------------------------------
- Integrated automatic rescheduling listener on activeGardenProvider.
- Whenever a plant is added, uprooted, or location/blinds level change, the
  app automatically updates the OS-level local notification alarms for
  dehydration warnings and growth milestones.

--------------------------------------------------------------------------------
[✓] Step 4: Affiliate Link Engine
--------------------------------------------------------------------------------
- Converted NativeAdWidget to Stateful + WidgetsBindingObserver.
- Tapping product recommendations pauses simulation loops immediately.
- Listens to app lifecycle; automatically unpauses the simulation when the
  player resumes the app from the external browser.
- Cleaned up failed redirect routes to avoid stuck pause states.
- Replaced mock URLs in ProductAd to point to windowgarden.net/shop/...

--------------------------------------------------------------------------------
Step 5: Asset Generation Pipeline (Blocked)
--------------------------------------------------------------------------------
- Blocked: Waiting for the Image Generation API quota reset (approx. 4 hours).
================================================================================
